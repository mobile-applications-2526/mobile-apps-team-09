# ===========================================
# PlantSense AI - Environment Configuration Template
# ===========================================
# INSTRUCTIONS FOR TEAM MEMBERS:
# 1. Copy this file: cp env.example .env
# 2. Update the values below with your actual credentials
# 3. Generate a secret key: openssl rand -hex 32
# 4. NEVER commit .env to git (it's already in .gitignore)

# ===========================================
# Application Settings
# ===========================================
APP_NAME=PlantSense AI Backend
APP_VERSION=1.0.0
DEBUG=True
ENVIRONMENT=development

# ===========================================
# Server Configuration
# ===========================================
HOST=0.0.0.0
PORT=8000

# ===========================================
# Database Configuration
# ===========================================
# For Docker Compose (recommended):
POSTGRES_USER=plantsense
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=plantsense_db
DATABASE_URL=postgresql+asyncpg://plantsense:plantsense@db:5432/plantsense_db

# For local development without Docker, you can use SQLite:
# Uncomment the line below and comment out the DATABASE_URL above
# DATABASE_URL=sqlite+aiosqlite:///./plantsense.db

# ===========================================
# Security Settings
# ===========================================
# CRITICAL: Generate a strong secret key!
# Run: openssl rand -hex 32
# Then paste the output below:
SECRET_KEY=REPLACE_WITH_OUTPUT_FROM_OPENSSL_RAND_HEX_32

ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===========================================
# CORS (Cross-Origin Resource Sharing)
# ===========================================
# Frontend URLs - adjust based on your setup:
# - http://localhost:3000 (React/Next.js)
# - http://localhost:8081 (React Native Metro)
# - http://localhost:19006 (Expo web)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081,http://localhost:19006
ALLOWED_METHODS=*
ALLOWED_HEADERS=*

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=INFO

# ===========================================
# pgAdmin (Database Management UI)
# ===========================================
PGADMIN_EMAIL=admin@plantsense.com
PGADMIN_PASSWORD=CHANGE_THIS_PASSWORD_TOO

# ===========================================
# PlantSense AI Specific Settings
# ===========================================
# Add these as you develop the AI features:

# AI/ML Model Configuration
# MODEL_PATH=/app/models/plant_detection_model.h5
# MODEL_VERSION=1.0.0
# CONFIDENCE_THRESHOLD=0.85

# Image Processing
# MAX_IMAGE_SIZE=10485760
# ALLOWED_IMAGE_TYPES=jpg,jpeg,png,webp
# IMAGE_QUALITY=85

# External APIs (uncomment when needed)
# OPENAI_API_KEY=your-openai-api-key-here
# HUGGING_FACE_TOKEN=your-hugging-face-token
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=plantsense-images

# Email Configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-specific-password

# Redis (for caching - add when needed)
# REDIS_URL=redis://localhost:6379/0

# ===========================================
# Development Settings
# ===========================================
# Disable in production!
RELOAD_ON_CHANGE=True
SHOW_SQL_QUERIES=False

